(()=>{const root=document.querySelector('#pure-plus-page'),thumbs=[...root.querySelectorAll('[data-gallery-thumb]')],main=root.querySelector('#pp-gallery-image'),caption=root.querySelector('#pp-gallery-caption'),zoom=root.querySelector('[data-gallery-zoom]'),dialog=root.querySelector('.pp-lightbox'),large=root.querySelector('#pp-lightbox-image'),largeCaption=root.querySelector('#pp-lightbox-caption');let current=0,ticket=0,previousOverflow='';
async function select(i){i=(i+thumbs.length)%thumbs.length;const item=thumbs[i],token=++ticket,img=new Image();img.src=item.href;try{await img.decode()}catch{return}if(token!==ticket)return;current=i;main.src=img.src;main.alt=item.dataset.caption;zoom.href=img.src;caption.textContent=`${i+1} / ${thumbs.length}`;large.src=img.src;large.alt=item.dataset.caption;largeCaption.textContent=`${i+1} / ${thumbs.length}`;thumbs.forEach((t,n)=>t.setAttribute('aria-current',String(n===i)))}
thumbs.forEach((t,i)=>t.addEventListener('click',e=>{e.preventDefault();select(i)}));root.querySelectorAll('[data-gallery-prev]').forEach(b=>{b.hidden=false;b.onclick=()=>select(current-1)});root.querySelectorAll('[data-gallery-next]').forEach(b=>{b.hidden=false;b.onclick=()=>select(current+1)});
zoom.addEventListener('click',e=>{e.preventDefault();large.src=main.src;large.alt=main.alt;largeCaption.textContent=`${current+1} / ${thumbs.length}`;previousOverflow=document.body.style.overflow;document.body.style.overflow='hidden';dialog.showModal()});root.querySelector('[data-gallery-close]').onclick=()=>dialog.close();dialog.addEventListener('close',()=>{document.body.style.overflow=previousOverflow;zoom.focus({preventScroll:true})});dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});root.querySelector('.pp-gallery').addEventListener('keydown',keys);dialog.addEventListener('keydown',keys);function keys(e){if(e.key==='ArrowRight'||e.key==='ArrowLeft'){e.preventDefault();select(current+(e.key==='ArrowRight'?1:-1))}}
let startX=0;large.addEventListener('touchstart',e=>startX=e.changedTouches[0].clientX,{passive:true});large.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>55)select(current+(dx<0?1:-1))},{passive:true});})();
